# Configuração do Banco de Dados
# Copie este arquivo para .env e preencha com suas credenciais do Neon (ou outro PostgreSQL)

# URL de conexão do PostgreSQL (Neon)
# Formato do Neon: postgresql://[USER]:[PASSWORD]@[ENDPOINT]/[DATABASE]?sslmode=require
# Exemplo: postgresql://neondb_owner:password@ep-xxxxx-pooler.region.aws.neon.tech/neondb?sslmode=require
# Para obter a URL: Neon Dashboard → Project → Connection String → URI
DATABASE_URL=postgresql://user:password@ep-xxxxx-pooler.region.aws.neon.tech/dbname?sslmode=require

# Configurações de Segurança JWT
SECRET_KEY=sua-chave-secreta-super-forte-aqui-use-openssl-rand-hex-32
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

